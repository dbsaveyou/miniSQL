
实现功能

插入索引
void insertIndex(string filePath,string key,int blockOffset,int type);
filePath 为文件名（含路径），key 为所建索引的键值，blockoffset 为根节点索引所指向的真实地址，type 为索引的种类
可以插入具体的某一条索引

删除索引
void deleteIndex(string filePath, string key,int type);
filePath 为文件名（含路径），key 为所建索引的键值，type为索引的种类。
可以删除某一表的具体的某一条索引

创建索引
void IndexManager::createIndex(string filePath,int type)；
filePath 为文件名（含路径），type为索引的种类。

销毁索引
void IndexManager::dropIndex(string filePath,int type)；
filePath 为文件名（含路径），type为索引的种类。


引用外界函数

List_TableAttribute
外界对索引进行创建和销毁时，由于传入了API中的Create_Index类和Drop_index类，所以引用了catalog的List_TableAttributes函数
来获得api表名所对应的索引种类。
